# Онбординг Call of the Ocean

## Описание

Приложение включает два экрана онбординга, которые показываются пользователю при первом запуске:

### Экран 1: "Discover. Decide. Evolve."
- **Логотип игры** в золотой рамке
- **Текст**: "Discover.", "Decide.", "Evolve."
- **Персонаж**: Sea King с приветственным жестом
- **Кнопка**: "Next"

### Экран 2: "Your Story Awaits Beneath the Surface"
- **Логотип игры** в золотой рамке
- **Текст**: "Your Story", "Awaits", "Beneath the", "Surface"
- **Персонаж**: Sea King с трезубцем
- **Кнопка**: "Start"

## Функциональность

### Автоматическое управление
- **Первый запуск**: Показываются оба экрана онбординга
- **Последующие запуски**: Показывается главное меню
- **Сохранение состояния**: Используется AsyncStorage

### Навигация
- **Экран 1 → Экран 2**: Кнопка "Next"
- **Экран 2 → Главное меню**: Кнопка "Start"
- **Автоматическое завершение**: После второго экрана

## Технические детали

### Компоненты
- `OnboardingScreen1` - Первый экран
- `OnboardingScreen2` - Второй экран
- `OnboardingFlow` - Управление потоком
- `OnboardingStorage` - Сохранение состояния

### Хранение данных
```typescript
// Ключ в AsyncStorage
const ONBOARDING_KEY = 'call_of_ocean_onboarding_completed';

// Методы
isOnboardingCompleted(): Promise<boolean>
setOnboardingCompleted(): Promise<void>
resetOnboarding(): Promise<void>
```

## Отладка

### Сброс онбординга
```javascript
// В консоли разработчика
OnboardingDebug.resetOnboarding();
```

### Проверка статуса
```javascript
// В консоли разработчика
OnboardingDebug.checkStatus();
```

### Принудительное завершение
```javascript
// В консоли разработчика
OnboardingDebug.completeOnboarding();
```

## Стилизация

### Цветовая схема
- **Фон**: Океанское изображение (OceanBackground)
- **Логотип**: Золотая рамка с темным фоном
- **Текст**: Желтый с тенью
- **Кнопки**: Золотой градиент с тенью
- **Персонаж**: Эмодзи Sea King (👑)

### Адаптивность
- **Отступы**: 60px сверху и снизу
- **Центрирование**: Все элементы по центру
- **Тени**: Для кнопок и текста

## Интеграция

### В Navigation.tsx
```typescript
const [currentScreen, setCurrentScreen] = useState<'onboarding' | 'main' | ...>('onboarding');

// Проверка статуса онбординга
const handleOnboardingComplete = () => {
  setCurrentScreen('main');
};
```

### В App.tsx
```typescript
import './src/utils/OnboardingDebug'; // Для отладки
```

## Тестирование

### Сценарии тестирования
1. **Первый запуск**: Должны показаться оба экрана
2. **Повторный запуск**: Должно показаться главное меню
3. **Сброс онбординга**: Должны снова показаться экраны
4. **Навигация**: Корректные переходы между экранами

### Команды для тестирования
```bash
# Запуск приложения
npm run ios
npm run android

# Сброс онбординга в консоли
OnboardingDebug.resetOnboarding();
```

## Будущие улучшения

### Возможные дополнения
- **Анимации переходов** между экранами
- **Индикатор прогресса** (1/2, 2/2)
- **Пропуск онбординга** для опытных пользователей
- **Локализация** текстов
- **Звуковые эффекты** для кнопок
