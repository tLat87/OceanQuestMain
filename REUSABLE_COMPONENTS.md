# Переиспользуемые компоненты Call of the Ocean

## ReusableButton

Универсальный компонент кнопки с поддержкой фоновых изображений и различных размеров.

### Пропсы

```typescript
interface ReusableButtonProps {
  onPress: () => void;           // Обработчик нажатия
  icon?: string;                 // Эмодзи или символ для иконки
  text?: string;                 // Текст кнопки
  style?: ViewStyle;             // Дополнительные стили
  textStyle?: TextStyle;         // Стили для текста
  size?: 'small' | 'medium' | 'large';  // Размер кнопки
  variant?: 'circular' | 'rectangular'; // Форма кнопки
  backgroundImage?: string;      // Путь к фоновому изображению
}
```

### Размеры

- **small**: 40x40px (радиус 20px)
- **medium**: 50x50px (радиус 25px) - по умолчанию
- **large**: 60x60px (радиус 30px)

### Варианты

- **circular**: Круглая кнопка (по умолчанию)
- **rectangular**: Прямоугольная кнопка с закругленными углами

### Примеры использования

```typescript
// Простая кнопка с иконкой
<ReusableButton
  icon="❓"
  onPress={handleHelp}
  size="medium"
/>

// Кнопка с текстом
<ReusableButton
  text="START MISSION"
  onPress={handleStart}
  variant="rectangular"
  size="large"
/>

// Кнопка с фоновым изображением
<ReusableButton
  icon="⭐"
  onPress={handleSave}
  backgroundImage="path/to/image.jpg"
  size="medium"
/>

// Маленькая кнопка "Назад"
<ReusableButton
  icon="←"
  onPress={handleBack}
  size="small"
  style={{ marginRight: 15 }}
/>
```

## Новые экраны

### NotificationScreen

Экран уведомлений с списком уведомлений и действиями.

**Функции:**
- Список уведомлений с заголовками и временем
- Индикатор непрочитанных уведомлений
- Кнопки действий (поделиться, очистить, настройки)

**Навигация:**
- Кнопка "Назад" возвращает в главное меню
- Кнопка "Настройки" (⚙️) в главном меню открывает уведомления

### StoriesHelpScreen

Экран помощи с доступными историями (показывается при нажатии на знак вопроса).

**Функции:**
- Список всех доступных историй
- Индикатор сложности (Beginner, Intermediate, Advanced)
- Описания историй
- Навигационные кнопки

**Навигация:**
- Кнопка "Помощь" (❓) в главном меню открывает этот экран
- Кнопка "Назад" возвращает в главное меню

## Обновленные экраны

Все существующие экраны обновлены для использования `ReusableButton`:

### MainMenuScreen
- Нижняя навигация: ❓, ⭐, ⚙️

### ScenarioSelectionScreen
- Кнопка "Назад" в хедере

### StoryScreen
- Кнопка "Назад" в хедере
- Навигационные кнопки: ←, 📤, ⭐, →

### QuestionScreen
- Кнопка "Назад" в хедере

### SavedStoriesScreen
- Кнопка "Назад" в хедере
- Навигационные кнопки: ←, 📤, ⭐, →

## Навигация

### Новые маршруты

1. **Главное меню → Уведомления**
   - Кнопка: ⚙️ (Settings)
   - Экран: `NotificationScreen`

2. **Главное меню → Помощь по историям**
   - Кнопка: ❓ (Help)
   - Экран: `StoriesHelpScreen`

### Обратная навигация

- **Уведомления → Главное меню**: Кнопка "Назад"
- **Помощь по историям → Главное меню**: Кнопка "Назад"

## Стилизация

### Цветовая схема
- **Фон кнопок**: Желтый (`colors.yellow`)
- **Текст/иконки**: Черный (`colors.black`)
- **Тени**: Стандартные тени для глубины

### Адаптивность
- Кнопки автоматически адаптируются под размер
- Поддержка различных форм (круглые/прямоугольные)
- Гибкая система стилей

## Преимущества

### Переиспользуемость
- Один компонент для всех кнопок
- Единообразный дизайн
- Легкое изменение стилей

### Гибкость
- Поддержка иконок и текста
- Различные размеры и формы
- Фоновые изображения

### Производительность
- Оптимизированный рендеринг
- Минимальные перерисовки
- Эффективное использование памяти

## Будущие улучшения

### Возможные дополнения
- **Анимации**: Плавные переходы при нажатии
- **Звуковые эффекты**: Звуки для кнопок
- **Haptic feedback**: Тактильная обратная связь
- **Темы**: Поддержка темной/светлой темы
- **Кастомизация**: Больше вариантов стилизации
