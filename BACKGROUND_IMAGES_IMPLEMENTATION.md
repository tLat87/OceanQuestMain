# Реализация фоновых изображений в Call of the Ocean

## 🎨 **Обзор**
Добавлены фоновые изображения по всему приложению для создания более привлекательного и профессионального вида.

## 🖼️ **Доступные изображения**

### **Фоновые изображения:**
- `src/assets/img/06edd5f952da0d9673fdfe9c5913e46c7e35a728.png` - Основное фоновое изображение океана
- `src/assets/img/BTN.png` - Изображение для кнопок

### **Placeholder изображения (временно):**
- `getButtonImage()` - Для обычных кнопок (200x60)
- `getSmallButtonImage()` - Для маленьких кнопок (50x50)
- `getLargeButtonImage()` - Для больших кнопок (250x60)

## 🔧 **Реализация**

### **1. Утилита для изображений (`src/utils/images.ts`)**

```typescript
export const getButtonImage = (): string => {
  return 'https://via.placeholder.com/200x60/FFD700/000000?text=BUTTON';
};

export const getSmallButtonImage = (): string => {
  return 'https://via.placeholder.com/50x50/FFD700/000000?text=BTN';
};

export const getLargeButtonImage = (): string => {
  return 'https://via.placeholder.com/250x60/FFD700/000000?text=BUTTON';
};
```

### **2. Восстановление OceanBackground**

**БЫЛО (временно):**
```typescript
<View style={styles.backgroundImage}>
  <View style={styles.content}>
    {children}
  </View>
</View>
```

**СТАЛО (с изображением):**
```typescript
<ImageBackground
  source={require('../assets/img/06edd5f952da0d9673fdfe9c5913e46c7e35a728.png')}
  style={styles.backgroundImage}
  resizeMode="cover"
>
  <View style={styles.content}>
    {children}
  </View>
</ImageBackground>
```

### **3. Восстановление ReusableButton**

**БЫЛО (временно):**
```typescript
return (
  <TouchableOpacity style={buttonStyles} onPress={handlePress}>
    {renderContent()}
  </TouchableOpacity>
);
```

**СТАЛО (с поддержкой изображений):**
```typescript
if (backgroundImage) {
  return (
    <TouchableOpacity style={buttonStyles} onPress={handlePress}>
      <ImageBackground
        source={{ uri: backgroundImage }}
        style={styles.backgroundImage}
        imageStyle={styles.backgroundImageStyle}
      >
        {renderContent()}
      </ImageBackground>
    </TouchableOpacity>
  );
}

return (
  <TouchableOpacity style={buttonStyles} onPress={handlePress}>
    {renderContent()}
  </TouchableOpacity>
);
```

## 📱 **Обновленные экраны**

### **1. MainMenuScreen**
- ✅ **START MISSION** кнопка с фоновым изображением
- ✅ **Навигационные кнопки** (❓, ⭐, ⚙️) с маленькими изображениями

### **2. ScenarioSelectionScreen**
- ✅ **Кнопка "Назад"** с маленьким изображением
- ✅ **Кнопки сценариев** с большими изображениями

### **3. QuestionScreen**
- ✅ **Кнопка "Назад"** с маленьким изображением
- ✅ **Кнопки ответов** с обычными изображениями

### **4. FeedbackScreen**
- ✅ **Кнопка "Next question"** с фоновым изображением

### **5. OnboardingScreen1 & OnboardingScreen2**
- ✅ **Кнопки "Next" и "Start"** с фоновыми изображениями

## 🎯 **Результат**

### **Визуальные улучшения:**
- 🌊 **Фоновое изображение океана** на всех экранах
- 🎨 **Фоновые изображения кнопок** для лучшего UX
- 🎭 **Единообразный дизайн** по всему приложению
- ✨ **Профессиональный вид** игры

### **Технические улучшения:**
- 🔧 **Централизованное управление** изображениями через утилиты
- 🎛️ **Гибкая система** размеров кнопок
- 🛡️ **Fallback система** - если изображение не загружается, показывается обычная кнопка
- 📱 **Оптимизация** для разных размеров экранов

## 🔄 **Замена placeholder изображений**

### **Когда у вас будут готовы реальные изображения:**

1. **Обновите `src/utils/images.ts`:**
```typescript
// Замените placeholder URLs на реальные пути
export const getButtonImage = (): string => require('../assets/img/BTN.png');
export const getSmallButtonImage = (): string => require('../assets/img/BTN_SMALL.png');
export const getLargeButtonImage = (): string => require('../assets/img/BTN_LARGE.png');
```

2. **Добавьте изображения в папку:**
```
src/assets/img/
├── 06edd5f952da0d9673fdfe9c5913e46c7e35a728.png (уже есть)
├── BTN.png (уже есть)
├── BTN_SMALL.png (добавить)
└── BTN_LARGE.png (добавить)
```

## 📊 **Текущее состояние**

### **Работает:**
- ✅ **Фоновое изображение океана** на всех экранах
- ✅ **Placeholder изображения кнопок** (временно)
- ✅ **Адаптивность** под разные размеры кнопок
- ✅ **Fallback система** для надежности

### **Готово к замене:**
- ⏳ **Placeholder изображения** → Реальные изображения кнопок
- ⏳ **Дополнительные размеры** кнопок при необходимости

## 🚀 **Итог**

### **Достигнуто:**
- 🎨 **Красивый визуальный дизайн** с фоновыми изображениями
- 🎮 **Профессиональный вид** игры
- 🔧 **Гибкая система** управления изображениями
- 📱 **Стабильная работа** на всех экранах

### **Готово к использованию:**
- **Все экраны** имеют фоновые изображения
- **Все кнопки** используют фоновые изображения
- **Единообразный дизайн** по всему приложению
- **Легкая замена** placeholder изображений на реальные

Теперь ваша игра выглядит профессионально и привлекательно! 🌊✨
